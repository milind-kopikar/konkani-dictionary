# Google Cloud SQL Environment Configuration
# Copy this to .env.gcp and customize for your Google Cloud SQL deployment

# Database Configuration (using Cloud SQL Proxy)
# Option 1: Full DATABASE_URL
DATABASE_URL=postgresql://your-user:your-password@127.0.0.1:5432/your-database?host=/cloudsql/your-project:your-region:your-instance

# Option 2: Individual variables (alternative to DATABASE_URL)
# PGHOST=/cloudsql/your-project:your-region:your-instance
# PGUSER=your-user
# PGPASSWORD=your-password
# PGDATABASE=your-database
# PGPORT=5432

# Environment
NODE_ENV=production

# API Configuration
API_BASE_URL=https://your-gcp-app.appspot.com/api

# Migration Safety
MIGRATE_ALLOW=false
MIGRATE_SECRET=your-secure-migration-secret

# SSL Configuration (Google Cloud SQL may require certificates)
# DB_SSL_CA=/secrets/cloudsql-ca.pem
# DB_SSL_CERT=/secrets/client-cert.pem
# DB_SSL_KEY=/secrets/client-key.pem

# Connection Pool (adjust for Cloud SQL latency)
DB_POOL_MAX=10
DB_IDLE_TIMEOUT=60000
DB_CONNECTION_TIMEOUT=10000

# Cloud SQL Proxy Configuration
# Ensure the proxy is running: ./cloud_sql_proxy -instances=your-project:your-region:your-instance=tcp:5432