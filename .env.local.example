# Local Development Environment Configuration
# Copy this to .env.local and customize for your local PostgreSQL setup

# Database Configuration (local PostgreSQL)
PGHOST=localhost
PGUSER=konkani_dev
PGPASSWORD=your-local-password
PGDATABASE=konkani_dictionary
PGPORT=5432

# Environment
NODE_ENV=development

# API Configuration
API_BASE_URL=http://localhost:3002/api

# Migration Safety (enabled for local development)
MIGRATE_ALLOW=true
MIGRATE_SECRET=local-dev-secret

# Connection Pool (smaller for local development)
DB_POOL_MAX=5
DB_IDLE_TIMEOUT=30000
DB_CONNECTION_TIMEOUT=2000

# No SSL for local development
# SSL settings are automatically disabled in development mode

# Optional: Custom ports
# PORT=3002

# Optional: CORS origins for development
# FRONTEND_URL=http://localhost:3000